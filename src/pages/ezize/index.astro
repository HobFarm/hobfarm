---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
import Breadcrumbs from "@/components/global/Breadcrumbs.astro";
import BeforeAfterCompare from "@/components/ui/BeforeAfterCompare.astro";
import EzizeEvidence from "@/components/workshop/EzizeEvidence.astro";
import { buildCompareItems } from "@/lib/cute-corrupted";
import { getMedia } from "@/data/media-registry";

const description =
  "EZIZE is HobFarm's private-alpha collectible image machine, built around structured random generation, exact recipe probabilities, and rendered easies.";
// The Access-protected private-alpha deployment has been verified at this address.
const app = {
  href: "https://ez.hob.farm/",
  customDomainReady: true,
};

const pairs = await buildCompareItems([
  "cute-corrupted/cakes",
  "cute-corrupted/raccoon",
  "cute-corrupted/sienna",
]);
const pairBySlug = new Map(pairs.map((pair) => [pair.slug, pair]));
const cabinetForms = [
  {
    name: "Cake",
    version: "v0.4",
    slug: "cute-corrupted/cakes",
    side: "after" as const,
    description:
      "Constructed edible objects resolved from cake type, tiers, flavors, decorations, presentation, and cake-specific variables.",
  },
  {
    name: "Critter",
    version: "v0.3",
    slug: "cute-corrupted/raccoon",
    side: "before" as const,
    description:
      "Animal-led collectible cartoons built from species identity, behavior, body logic, environment, and modifiers.",
  },
  {
    name: "Character",
    version: "v0.1",
    slug: "cute-corrupted/sienna",
    side: "after" as const,
    description:
      "Humanoid and persona-driven figures with identity, role, wardrobe, action, props, body traits, conditions, environments, and modifiers.",
  },
].flatMap((form) => {
  const pair = pairBySlug.get(form.slug);
  return pair ? [{ ...form, pair }] : [];
});

const ezizeOutput = getMedia("ezize.output.corrupted-cake");
const socialImage = ezizeOutput.src;
const siteOrigin = Astro.site?.origin ?? "https://hob.farm";
const canonicalUrl = new URL("/ezize/", siteOrigin).toString();
const structuredData = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Organization",
      "@id": `${siteOrigin}/#organization`,
      name: "HobFarm",
      url: `${siteOrigin}/`,
    },
    {
      "@type": "WebSite",
      "@id": `${siteOrigin}/#website`,
      name: "HobFarm",
      url: `${siteOrigin}/`,
      publisher: { "@id": `${siteOrigin}/#organization` },
    },
    {
      "@type": "WebPage",
      "@id": `${canonicalUrl}#webpage`,
      url: canonicalUrl,
      name: "EZIZE | HobFarm",
      description,
      isPartOf: { "@id": `${siteOrigin}/#website` },
      publisher: { "@id": `${siteOrigin}/#organization` },
      mainEntity: { "@id": `${canonicalUrl}#ezize` },
      ...(socialImage ? { image: socialImage } : {}),
    },
    {
      "@type": "CreativeWork",
      "@id": `${canonicalUrl}#ezize`,
      name: "EZIZE",
      alternateName: "easies",
      description,
      url: canonicalUrl,
      creator: { "@id": `${siteOrigin}/#organization` },
      publisher: { "@id": `${siteOrigin}/#organization` },
      genre: ["Collectible image system", "Generative art"],
    },
  ],
};

const pullSteps = [
  "Pull",
  "Resolve traits",
  "Calculate probability",
  "Build image specification",
  "Render image",
  "Record and download",
];
const rarityRecipe = ["Cute", "Character", "Alien", "Chef", "Decorating a cake", "Melting condition"];
---

<BaseLayout
  title="EZIZE | HobFarm"
  description={description}
  canonical="/ezize/"
  image={socialImage}
  imageAlt={ezizeOutput.alt}
  structuredData={structuredData}
  palette="magenta-cyan"
>
  <div class="ezize-page">
    <section class="hero" aria-labelledby="ezize-title">
      <Wrapper variant="standard" class="hero-wrap">
        <Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "EZIZE" }]} />
        <div class="hero-grid">
          <div class="hero-copy">
            <p class="eyebrow">A collectible image system published by HobFarm</p>
            <h1 id="ezize-title">EZIZE</h1>
            <p class="pronunciation"><span>Private alpha</span> Pronounced “easies.”</p>
            <p class="deck">
              A probability-driven collectible image machine. Every pull resolves a structured combination of
              traits, calculates how likely that exact recipe was, builds the image specification, and renders an easie.
            </p>
            <div class="hero-actions">
              {app.customDomainReady && (
                <a class="primary-action" href={app.href}>Open EZIZE · Private alpha <span aria-hidden="true">&rarr;</span></a>
              )}
              <a class="secondary-action" href="#cabinet">Open the cabinet</a>
            </div>
            <p class="app-home">
              Private-alpha application: <span>{app.href.replace("https://", "")}</span>. Owner access only;
              Cloudflare Access protects the application, and public generation is not open.
            </p>
          </div>

          <div class="hero-evidence"><EzizeEvidence priority /></div>
        </div>
      </Wrapper>
    </section>

    <section class="definition" aria-labelledby="definition-heading">
      <Wrapper variant="standard" class="section-space">
        <div class="split-heading">
          <div>
            <p class="eyebrow">What is an EZIZE?</p>
            <h2 id="definition-heading">One pull. One recipe. One easie.</h2>
          </div>
          <div class="body-copy">
            <p>
              An <strong>ezize</strong>, or <strong>easie</strong>, is one collectible produced by the machine. The
              recipe comes first. The image model does not choose the rarity, the form, or the underlying traits.
            </p>
            <p>
              The private alpha now runs the complete path from compiled Grimoire packs through Wildcard Machine,
              canonical image JSON, the EZIZE prompt compiler, and OpenAI GPT Image. The first development proof
              rendered a 2336×3504 portrait PNG from a resolved Cute Character recipe. The current Corrupted Cake
              proof completes the same path and can be downloaded as a PNG from the owner view.
            </p>
          </div>
        </div>

        <ol class="pull-path" aria-label="Conceptual EZIZE pull path">
          {pullSteps.map((step, index) => (
            <li><span>{String(index + 1).padStart(2, "0")}</span><strong>{step}</strong></li>
          ))}
        </ol>
        <p class="diagram-note">Working private-alpha path. Public accounts, retail credits, and public generation access are not available.</p>
      </Wrapper>
    </section>

    <section class="lineage" aria-labelledby="lineage-heading">
      <Wrapper variant="standard" class="section-space">
        <div class="split-heading">
          <div>
            <p class="eyebrow">Project lineage</p>
            <h2 id="lineage-heading">A visual series became a working application.</h2>
          </div>
          <div class="body-copy">
            <p>
              Cute &amp; Corrupted established the paired visual premise. Grimoire turned its concepts into authored
              taxonomies and versioned packs. Wildcard Machine supplied the seeded probability runtime. EZIZE brought
              those systems together with an interface, image compiler, renderer, download, and development record.
            </p>
            <p>
              The final application was not fully specified when the first paired images were made. Each stage exposed
              the next useful problem and preserved enough structure for that new branch to become possible.
            </p>
          </div>
        </div>

        <ol class="lineage-chain" aria-label="Systems that converged into EZIZE">
          <li><span>01 / origin</span><strong>Cute &amp; Corrupted</strong><p>Persona and transformation grammar</p></li>
          <li><span>02 / authoring</span><strong>Grimoire</strong><p>Taxonomies, weights, and compiled packs</p></li>
          <li><span>03 / runtime</span><strong>Wildcard Machine</strong><p>Deterministic resolution and probability</p></li>
          <li><span>04 / application</span><strong>EZIZE</strong><p>Pull, render, record, and PNG download</p></li>
        </ol>
      </Wrapper>
    </section>

    <section class="blind-pull" aria-labelledby="blind-pull-heading">
      <Wrapper variant="standard" class="section-space">
        <div class="blind-grid">
          <div>
            <p class="eyebrow">You do not choose what comes out</p>
            <h2 id="blind-pull-heading">Pull the handle. Let the cabinet decide.</h2>
            <p class="deck-small">
              EZIZE works like a vending machine, a gumball machine, or a sealed trading-card pack. It is not a
              conventional prompt form. The structured surprise is the point. Common pulls still need to make good
              pictures; unusual combinations emerge naturally when several weighted choices meet.
            </p>
          </div>
          <ol class="arcade-sequence">
            <li><span>01</span><div><strong>Start with a seed</strong><p>The seed makes the pull repeatable.</p></div></li>
            <li><span>02</span><div><strong>Resolve the branches</strong><p>Cabinet, persona, form, and traits follow their defined weights.</p></div></li>
            <li><span>03</span><div><strong>Seal the recipe</strong><p>The genome, probability, and image specification are recorded together.</p></div></li>
          </ol>
        </div>
      </Wrapper>
    </section>

    <section id="cabinet" class="cabinet-section" aria-labelledby="cabinet-heading">
      <Wrapper variant="standard" class="section-space">
        <header class="section-heading">
          <p class="eyebrow">The Cabinet</p>
          <h2 id="cabinet-heading">Three forms are in the machine.</h2>
          <p>
            Each form owns its variables. A cake does not borrow a character wardrobe, and a critter does not use a
            cake-tier rule. The Cabinet chooses the correct grammar before the pull continues.
          </p>
        </header>

        <div class="form-grid">
          {cabinetForms.map((form, index) => {
            const image = form.pair[form.side];
            return (
              <article class="form-card">
                <a href={form.pair.href} aria-label={`See the ${form.name} source study in the HobFarm Gallery`}>
                  <div class="form-image"><img src={image.src} alt={image.alt} loading="lazy" decoding="async" /></div>
                  <div class="form-copy">
                    <div><span>{String(index + 1).padStart(2, "0")}</span><b>{form.version}</b></div>
                    <h3>{form.name}</h3>
                    <p>{form.description}</p>
                    <strong>See the source study <span aria-hidden="true">→</span></strong>
                  </div>
                </a>
              </article>
            );
          })}
        </div>
      </Wrapper>
    </section>

    {pairBySlug.get("cute-corrupted/raccoon") && (
      <section class="modes" aria-labelledby="modes-heading">
        <Wrapper variant="standard" class="section-space">
          <div class="modes-grid">
            <div class="compare-wrap">
              <BeforeAfterCompare
                before={pairBySlug.get("cute-corrupted/raccoon")!.before}
                after={pairBySlug.get("cute-corrupted/raccoon")!.after}
                video={pairBySlug.get("cute-corrupted/raccoon")!.video}
                aspect={pairBySlug.get("cute-corrupted/raccoon")!.aspect}
                ariaLabel="Raccoon in Cute and Corrupted modes"
              />
            </div>
            <div>
              <p class="eyebrow">Cute / Corrupted</p>
              <h2 id="modes-heading">Two intentional modes.</h2>
              <p class="mode-intro">
                Cute and Corrupted began as the public name of the project. They now operate inside EZIZE as a core
                persona and transformation grammar.
              </p>
              <dl class="mode-definitions">
                <div><dt>Cute</dt><dd>Readable, charming, polished, and inviting.</dd></div>
                <div><dt>Corrupted</dt><dd>Darker, stranger, theatrical, toxic, supernatural, warped, excessive, or otherwise intensified.</dd></div>
              </dl>
              <p class="mode-note">
                Corrupted owns the transformation. It is not the failed version of a Cute image. Skeletons, aliens,
                robots, and other unusual identities can appear in either mode.
              </p>
              <a class="text-link" href="/workshop/cute-and-corrupted/">Read the Workshop method <span aria-hidden="true">→</span></a>
            </div>
          </div>
        </Wrapper>
      </section>
    )}

    <section class="rarity" aria-labelledby="rarity-heading">
      <Wrapper variant="standard" class="section-space">
        <div class="split-heading rarity-heading">
          <div>
            <p class="eyebrow">Rarity comes from the machine</p>
            <h2 id="rarity-heading">There is no “Legendary” die roll.</h2>
          </div>
          <div class="body-copy">
            <p>
              Every choice in the generation path has a probability. Those probabilities multiply, so rarity emerges
              from the completed recipe. A common trait can become part of an extremely unusual combination. The first
              successful render resolved a feminine-coded alien chef decorating a cake, with stylized melting edges.
              It remained a Character recipe; the cake was part of the action, not a switch into the Cake form.
            </p>
            <p>
              The engine records exact full-genome probability. Highly detailed genomes can produce enormous “1 in X”
              values, so the application will need a careful collector-facing summary. The number describes the recipe,
              not the observed population, visual distinction, or collector preference.
            </p>
          </div>
        </div>

        <div class="recipe" aria-label="Example of traits combining into an unusual recipe">
          {rarityRecipe.map((trait, index) => (
            <div><span>{index === 0 ? "" : "×"}</span><strong>{trait}</strong></div>
          ))}
          <div class="recipe-result"><span>=</span><strong>One extremely unusual recipe</strong></div>
        </div>
        <div class="rarity-separations" aria-label="Rarity distinctions">
          <span>Recipe probability</span><b>≠</b><span>Observed population</span><b>≠</b><span>Visual distinction</span><b>≠</b><span>Collector preference</span>
        </div>
      </Wrapper>
    </section>

    <section class="system" aria-labelledby="system-heading">
      <Wrapper variant="standard" class="section-space">
        <header class="section-heading">
          <p class="eyebrow">Inside the machine</p>
          <h2 id="system-heading">The image model receives a resolved specification.</h2>
          <p>
            HobFarm publishes EZIZE. Three supporting systems do separate jobs, so the collectible structure exists
            before an image provider draws the final surface.
          </p>
        </header>

        <div class="system-map">
          <div class="publisher-node"><span>Publisher</span><strong>HobFarm</strong><p>Publishes and develops EZIZE.</p></div>
          <div class="product-node"><span>Product</span><strong>EZIZE</strong><p>Creates and records the collectible recipe.</p></div>
          <div class="engine-nodes">
            <article><span>Authoring and schema</span><h3>Grimoire</h3><p>Defines what can exist.</p></article>
            <article><span>Deterministic runtime</span><h3>Wildcard Machine</h3><p>Performs the seeded pull and calculates its probability.</p></article>
            <article><span>Image renderer</span><h3>OpenAI</h3><p>Renders the already-resolved visual specification in the private alpha.</p></article>
          </div>
        </div>
      </Wrapper>
    </section>

    <section class="representation" aria-labelledby="representation-heading">
      <Wrapper variant="standard" class="section-space">
        <div class="split-heading">
          <div>
            <p class="eyebrow">Representation between stages</p>
            <h2 id="representation-heading">A schema is not necessarily JSON.</h2>
          </div>
          <div class="body-copy">
            <p>
              A schema is the explicit structure that lets the next stage understand the decisions that matter. JSON
              is one serialization. EZIZE also depends on concept taxonomies, persona and form grammar, weighted trait
              relationships, deterministic identity, image direction, and application state.
            </p>
            <p>
              Each representation has a job. The image provider receives a resolved visual specification. It does not
              decide the Cabinet, persona, form, traits, or probability that produced the recipe.
            </p>
          </div>
        </div>

        <dl class="representation-grid">
          <div><dt>Authored grammar</dt><dd>Cabinet, persona, form, trait relationships, weights, and exclusions define what can exist.</dd></div>
          <div><dt>Deterministic identity</dt><dd>Seed, resolved genome, and exact full-genome probability identify the completed pull.</dd></div>
          <div><dt>Image contract</dt><dd>Canonical image JSON and the prompt compiler translate the recipe into a renderable specification.</dd></div>
          <div><dt>Development record</dt><dd>EZIZE ID, render state, output dimensions, image hash, and PNG provenance fields stay attached to the owner-alpha result.</dd></div>
        </dl>
        <p class="representation-note">The development record is working. Public accounts, personal collections, and gallery curation are future product work.</p>
      </Wrapper>
    </section>

    <section class="status" aria-labelledby="status-heading">
      <Wrapper variant="standard" class="section-space">
        <header class="section-heading">
          <p class="eyebrow">Current build status</p>
          <h2 id="status-heading">The machine is rendering in private alpha.</h2>
        </header>
        <div class="status-grid">
          <article>
            <span class="status-label available">Working</span>
            <h3>Deterministic pull</h3>
            <p>Compiled packs → seed → Cabinet → persona → form → traits → genome → exact probability → canonical image JSON.</p>
            <ul><li>Cake v0.4</li><li>Critter v0.3</li><li>Character v0.1</li></ul>
          </article>
          <article>
            <span class="status-label available">Working</span>
            <h3>Render and PNG download</h3>
            <p>The EZIZE prompt compiler sends the resolved specification to OpenAI GPT Image, receives the rendered easie, and exposes a PNG download in the owner view.</p>
            <ul><li>Current proof: Corrupted Cake</li><li>2336×3504 portrait PNG</li><li>Owner access only</li></ul>
          </article>
          <article>
            <span class="status-label building">Development record</span>
            <h3>Identity and provenance fields</h3>
            <p>The owner view records the seed, genome, exact probability, render state, output dimensions, hashes, and development PNG provenance fields.</p>
            <ul><li>Local to the owner alpha</li><li>Identifies the recipe and file</li><li>Not a public collection</li></ul>
          </article>
          <article>
            <span class="status-label planned">Planned</span>
            <h3>Public product layer</h3>
            <p>Retail credits, public accounts, personal collections, public generation, agent tools, and downloadable easies.</p>
          </article>
        </div>
      </Wrapper>
    </section>

    <section class="collecting" aria-labelledby="collecting-heading">
      <Wrapper variant="standard" class="section-space">
        <div class="collect-grid">
          <div>
            <p class="eyebrow">Generated, recorded, and kept.</p>
            <h2 id="collecting-heading">The file is already the collectible.</h2>
            <p>
              An easie is a digital collectible with a structured identity, an exact recipe, and a record of how its
              image was produced. The planned public path lets a person generate, download, keep, and organize selected
              easies without changing what the work is.
            </p>
          </div>
          <div class="future-chain">
            <div><span>01</span><strong>EZIZE</strong><p>Creates and records the collectible.</p></div>
            <div><span>02</span><strong>PNG</strong><p>Stores the image and its portable development metadata.</p></div>
            <div><span>03 / planned</span><strong>Collection</strong><p>Organizes the easies a person chooses to keep.</p></div>
          </div>
        </div>
        <p class="collect-note">
          Public accounts and collections are not live. HobFarm remains the publisher and creator identity; EZIZE is
          the collection and product identity.
        </p>
      </Wrapper>
    </section>

    <section class="name-section" aria-labelledby="name-heading">
      <Wrapper variant="standard" class="section-space">
        <div class="name-grid">
          <div><p class="mini-wordmark" aria-hidden="true">EZIZE</p></div>
          <div>
            <p class="eyebrow">Why EZIZE?</p>
            <h2 id="name-heading">Easy to pull. Easies to collect.</h2>
            <p>
              EZ carries the “easy” association. EZIZE is pronounced “easies,” which is also what the individual
              collectibles are called. The product is EZIZE. The thing you receive is an ezize, or an easie.
            </p>
          </div>
        </div>
      </Wrapper>
    </section>

    <section class="final-cta" aria-labelledby="final-heading">
      <Wrapper variant="standard" class="section-space">
        <p class="eyebrow">Private alpha</p>
        <h2 id="final-heading">EZIZE is working behind restricted access.</h2>
        <p>
          The machine can resolve and render real easies. The application is live behind Cloudflare Access and remains
          owner-only during private alpha. Public accounts and generation access do not exist yet. The original Cute /
          Corrupted studies remain available in the HobFarm Gallery, with the production method in Workshop.
        </p>
        <div class="hero-actions final-actions">
          {app.customDomainReady && (
            <a class="primary-action" href={app.href}>Open EZIZE · Private alpha <span aria-hidden="true">&rarr;</span></a>
          )}
          <a class="secondary-action" href="/gallery/cute-corrupted/raccoon/">See a Critter study</a>
          <a class="secondary-action" href="/workshop/cute-and-corrupted/">Read the Workshop method</a>
          <a class="secondary-action" href="/workshop/">Visit HobFarm Workshop</a>
        </div>
      </Wrapper>
    </section>
  </div>
</BaseLayout>

<style>
  .ezize-page {
    --ez-line: #332940;
    --ez-panel: #0d0a13;
    --ez-paper: #ece9f5;
    --ez-muted: #aaa5b8;
    --ez-cyan: #63e9eb;
    --ez-pink: #e660a7;
    overflow: hidden;
    background: #07060b;
    color: var(--ez-muted);
  }
  .ezize-page section { border-bottom: 1px solid var(--ez-line); }
  :global(.ezize-page .section-space) { padding-block: clamp(4.75rem, 9vw, 8rem); }
  .eyebrow { margin: 0; color: var(--ez-cyan); font-family: var(--font-mono); font-size: .64rem; letter-spacing: .18em; text-transform: uppercase; }
  h1, h2, h3 { color: var(--ez-paper); font-family: var(--font-display); font-weight: 300; }
  h2 { max-width: 16ch; margin: .95rem 0 0; font-size: clamp(2.6rem, 6vw, 5.8rem); letter-spacing: -.055em; line-height: .92; text-wrap: balance; }
  p { line-height: 1.72; }
  strong { color: #ded9e6; }
  .hero { background: radial-gradient(circle at 86% 18%, rgb(99 233 235 / .11), transparent 32rem), radial-gradient(circle at 12% 72%, rgb(230 96 167 / .12), transparent 30rem), #07060b; }
  :global(.hero-wrap) { padding-top: 1.3rem; padding-bottom: clamp(4rem, 8vw, 7rem); }
  .hero-grid { display: grid; gap: clamp(3rem, 7vw, 6rem); margin-top: clamp(3.5rem, 8vw, 6rem); align-items: center; }
  .hero-copy { min-width: 0; max-width: 58rem; }
  .hero-evidence { min-width: 0; max-width: 72rem; }
  h1 { margin: 1.1rem 0 0; color: white; font-size: clamp(5rem, 17vw, 12.5rem); font-weight: 650; letter-spacing: -.095em; line-height: .73; }
  .pronunciation { margin: 1.35rem 0 0; color: var(--ez-pink); font-family: var(--font-mono); font-size: .7rem; letter-spacing: .14em; text-transform: uppercase; }
  .deck { max-width: 47rem; margin: 1.7rem 0 0; color: #d2cdda; font-size: clamp(1.04rem, 2vw, 1.25rem); line-height: 1.75; }
  .hero-actions { display: flex; flex-wrap: wrap; gap: .75rem; margin-top: 2rem; }
  .primary-action, .secondary-action { display: inline-flex; min-height: 2.9rem; align-items: center; justify-content: center; padding: .72rem 1.1rem; border: 1px solid var(--ez-line); border-radius: 999px; font-family: var(--font-mono); font-size: .62rem; letter-spacing: .06em; text-decoration: none; text-transform: uppercase; }
  .primary-action { border-color: var(--ez-cyan); background: var(--ez-cyan); color: #071011; }
  .primary-action:not(.is-disabled):hover, .primary-action:not(.is-disabled):focus-visible { background: white; border-color: white; outline: none; }
  .primary-action.is-disabled { border-color: #47404e; background: #17131b; color: #857e8d; cursor: not-allowed; }
  .secondary-action { color: #dcd7e3; }
  .secondary-action:hover, .secondary-action:focus-visible { border-color: var(--ez-pink); color: white; outline: none; }
  .app-home { margin: 1rem 0 0; color: #777181; font-family: var(--font-mono); font-size: .62rem; letter-spacing: .04em; }
  .app-home span { color: #aaa5b8; }
  .split-heading { display: grid; gap: clamp(2rem, 5vw, 5rem); }
  .body-copy { max-width: 46rem; }
  .body-copy p { margin: 0; }
  .body-copy p + p { margin-top: 1.15rem; }
  .pull-path { display: grid; gap: .65rem; margin: clamp(2.5rem, 5vw, 4rem) 0 0; padding: 0; list-style: none; }
  .pull-path li { min-width: 0; min-height: 7.5rem; padding: 1rem; border: 1px solid var(--ez-line); background: var(--ez-panel); }
  .pull-path li span { display: block; color: var(--ez-cyan); font-family: var(--font-mono); font-size: .56rem; }
  .pull-path li strong { display: block; margin-top: 2rem; font-size: .74rem; font-weight: 500; line-height: 1.45; }
  .diagram-note { margin: 1rem 0 0; color: #777181; font-family: var(--font-mono); font-size: .58rem; letter-spacing: .03em; }
  .lineage { background: radial-gradient(circle at 12% 45%, rgb(230 96 167 / .075), transparent 30rem), #09070d; }
  .lineage-chain { display: grid; gap: .7rem; margin: clamp(2.5rem, 5vw, 4rem) 0 0; padding: 0; list-style: none; }
  .lineage-chain li { position: relative; min-width: 0; padding: 1.15rem; border: 1px solid var(--ez-line); background: #0c0910; }
  .lineage-chain span { color: var(--ez-cyan); font-family: var(--font-mono); font-size: .55rem; letter-spacing: .08em; text-transform: uppercase; }
  .lineage-chain strong { display: block; margin-top: 1.5rem; font-family: var(--font-display); font-size: clamp(1.55rem, 3vw, 2.15rem); font-weight: 350; line-height: 1; }
  .lineage-chain p { margin: .75rem 0 0; color: #958f9e; font-size: .74rem; }
  .blind-pull { background: linear-gradient(90deg, rgb(230 96 167 / .045), transparent 40%), #0a0810; }
  .blind-grid { display: grid; gap: clamp(3rem, 7vw, 6rem); }
  .deck-small { max-width: 45rem; margin: 1.5rem 0 0; }
  .arcade-sequence { display: grid; gap: .7rem; margin: 0; padding: 0; list-style: none; }
  .arcade-sequence li { display: grid; grid-template-columns: 3rem minmax(0, 1fr); gap: 1rem; align-items: start; padding: 1.2rem; border: 1px solid var(--ez-line); background: #08060b; }
  .arcade-sequence li > span { color: var(--ez-pink); font-family: var(--font-mono); font-size: .6rem; }
  .arcade-sequence strong { font-weight: 500; }
  .arcade-sequence p { margin: .45rem 0 0; color: #8f8999; font-size: .78rem; }
  .section-heading { max-width: 62rem; }
  .section-heading > p:last-child { max-width: 49rem; margin: 1.5rem 0 0; }
  .form-grid { display: grid; gap: 1rem; margin-top: clamp(2.5rem, 5vw, 4rem); }
  .form-card { min-width: 0; overflow: hidden; border: 1px solid var(--ez-line); border-radius: .7rem; background: var(--ez-panel); }
  .form-card > a { display: flex; height: 100%; flex-direction: column; color: inherit; text-decoration: none; }
  .form-card:hover, .form-card:focus-within { border-color: var(--ez-cyan); }
  .form-card > a:focus-visible { outline: 2px solid var(--ez-cyan); outline-offset: -2px; }
  .form-image { overflow: hidden; border-bottom: 1px solid var(--ez-line); background: #050407; }
  .form-image img { display: block; width: 100%; aspect-ratio: 4 / 5; object-fit: cover; transition: filter .25s ease, transform .4s ease; }
  .form-card:hover img, .form-card:focus-within img { filter: brightness(1.08); transform: scale(1.02); }
  .form-copy { display: flex; flex: 1; flex-direction: column; padding: 1.35rem; }
  .form-copy > div { display: flex; justify-content: space-between; color: var(--ez-cyan); font-family: var(--font-mono); font-size: .56rem; letter-spacing: .08em; text-transform: uppercase; }
  .form-copy > div b { color: var(--ez-pink); font-weight: 400; }
  .form-copy h3 { margin: 1.4rem 0 0; font-size: clamp(2.1rem, 5vw, 3.4rem); letter-spacing: -.04em; line-height: .9; }
  .form-copy p { margin: 1.2rem 0 0; color: #9a94a4; font-size: .84rem; }
  .form-copy > strong { margin-top: auto; padding-top: 2rem; color: var(--ez-cyan); font-family: var(--font-mono); font-size: .58rem; font-weight: 400; letter-spacing: .05em; text-transform: uppercase; }
  .modes { background: radial-gradient(circle at 7% 55%, rgb(99 233 235 / .07), transparent 28rem), #09070d; }
  .modes-grid { display: grid; gap: clamp(3rem, 7vw, 6rem); align-items: center; }
  .compare-wrap { min-width: 0; }
  .mode-intro, .mode-note { max-width: 43rem; margin: 1.5rem 0 0; }
  .mode-definitions { display: grid; gap: .7rem; margin: 2rem 0 0; }
  .mode-definitions > div { padding: 1rem; border-left: 3px solid var(--ez-cyan); background: #0d0a13; }
  .mode-definitions > div:last-child { border-left-color: var(--ez-pink); }
  .mode-definitions dt { color: white; font-family: var(--font-display); font-size: 1.35rem; }
  .mode-definitions dd { margin: .45rem 0 0; color: #9892a2; line-height: 1.6; }
  .text-link { display: inline-block; margin-top: 1.7rem; color: var(--ez-cyan); font-family: var(--font-mono); font-size: .6rem; letter-spacing: .05em; text-decoration: none; text-transform: uppercase; }
  .text-link:hover, .text-link:focus-visible { color: white; }
  .rarity { background: #0a0810; }
  .recipe { display: flex; flex-wrap: wrap; gap: .55rem; align-items: stretch; margin-top: clamp(2.5rem, 5vw, 4rem); }
  .recipe > div { display: flex; min-height: 3.6rem; align-items: center; gap: .55rem; padding: .7rem .85rem; border: 1px solid var(--ez-line); background: #08060b; }
  .recipe span { color: var(--ez-pink); font-family: var(--font-mono); }
  .recipe strong { font-size: .72rem; font-weight: 500; }
  .recipe .recipe-result { border-color: #4a4154; background: #15101a; }
  .recipe .recipe-result strong { color: var(--ez-cyan); }
  .rarity-separations { display: flex; flex-wrap: wrap; gap: .65rem; align-items: center; margin-top: 1rem; color: #918b9c; font-family: var(--font-mono); font-size: .62rem; letter-spacing: .04em; text-transform: uppercase; }
  .rarity-separations b { color: var(--ez-pink); font-size: 1rem; font-weight: 400; }
  .system-map { position: relative; display: grid; gap: .8rem; margin-top: clamp(2.5rem, 5vw, 4rem); }
  .publisher-node, .product-node, .engine-nodes article { position: relative; padding: 1.2rem; border: 1px solid var(--ez-line); background: var(--ez-panel); }
  .publisher-node::after, .product-node::after { position: absolute; bottom: -.85rem; left: 50%; width: 1px; height: .85rem; background: var(--ez-pink); content: ""; }
  .publisher-node span, .product-node span, .engine-nodes article span { display: block; color: var(--ez-cyan); font-family: var(--font-mono); font-size: .54rem; letter-spacing: .08em; text-transform: uppercase; }
  .publisher-node strong, .product-node strong { display: block; margin-top: .8rem; color: white; font-family: var(--font-display); font-size: 2rem; font-weight: 300; }
  .publisher-node p, .product-node p, .engine-nodes article p { margin: .6rem 0 0; color: #8f8998; font-size: .76rem; }
  .product-node { border-color: #694058; background: linear-gradient(90deg, rgb(230 96 167 / .08), transparent), #0d0a13; }
  .engine-nodes { display: grid; gap: .8rem; }
  .engine-nodes h3 { margin: 1rem 0 0; font-size: clamp(1.5rem, 3vw, 2.2rem); letter-spacing: -.03em; }
  .representation { background: linear-gradient(120deg, rgb(99 233 235 / .04), transparent 45%), #0a0810; }
  .representation-grid { display: grid; gap: .8rem; margin: clamp(2.5rem, 5vw, 4rem) 0 0; }
  .representation-grid > div { min-width: 0; padding: 1.2rem; border: 1px solid var(--ez-line); background: var(--ez-panel); }
  .representation-grid dt { color: var(--ez-cyan); font-family: var(--font-mono); font-size: .57rem; letter-spacing: .08em; text-transform: uppercase; }
  .representation-grid dd { margin: 1.25rem 0 0; color: #aaa4b2; font-size: .8rem; line-height: 1.65; }
  .representation-note { max-width: 58rem; margin: .8rem 0 0; padding: 1rem; border-left: 3px solid var(--ez-pink); background: rgb(230 96 167 / .04); color: #aaa4b3; }
  .status { background: linear-gradient(135deg, rgb(99 233 235 / .035), transparent 48%), #09070d; }
  .status-grid { display: grid; gap: 1rem; margin-top: clamp(2.5rem, 5vw, 4rem); }
  .status-grid article { min-width: 0; padding: 1.35rem; border: 1px solid var(--ez-line); background: var(--ez-panel); }
  .status-label { display: inline-flex; padding: .35rem .55rem; border: 1px solid #4d4354; border-radius: 999px; color: #a39cab; font-family: var(--font-mono); font-size: .52rem; letter-spacing: .08em; text-transform: uppercase; }
  .status-label.available { border-color: rgb(99 233 235 / .45); color: var(--ez-cyan); }
  .status-label.building { border-color: rgb(230 96 167 / .45); color: var(--ez-pink); }
  .status-grid h3 { margin: 1.6rem 0 0; font-size: 1.8rem; letter-spacing: -.03em; }
  .status-grid p { margin: 1rem 0 0; color: #96909f; font-size: .82rem; overflow-wrap: anywhere; }
  .status-grid p span { color: #d3ced9; font-family: var(--font-mono); font-size: .72rem; }
  .status-grid ul { display: flex; flex-wrap: wrap; gap: .45rem; margin: 1.2rem 0 0; padding: 0; list-style: none; }
  .status-grid li { padding: .35rem .5rem; border: 1px solid #3c3344; color: #a49ead; font-family: var(--font-mono); font-size: .56rem; }
  .collecting { background: #0a0810; }
  .collect-grid { display: grid; gap: clamp(3rem, 7vw, 6rem); }
  .collect-grid > div:first-child > p:last-child { max-width: 47rem; margin: 1.5rem 0 0; }
  .future-chain { display: grid; gap: .75rem; }
  .future-chain > div { padding: 1.15rem; border: 1px solid var(--ez-line); background: #08060b; }
  .future-chain span { display: block; color: var(--ez-pink); font-family: var(--font-mono); font-size: .55rem; letter-spacing: .07em; text-transform: uppercase; }
  .future-chain strong { display: block; margin-top: .8rem; color: white; font-family: var(--font-display); font-size: 1.55rem; font-weight: 300; }
  .future-chain p { margin: .55rem 0 0; color: #918b9b; font-size: .76rem; }
  .collect-note { margin: 2rem 0 0; padding: 1rem; border-left: 3px solid var(--ez-pink); background: rgb(230 96 167 / .045); color: #aaa4b3; }
  .name-section { background: radial-gradient(circle at 15% 45%, rgb(230 96 167 / .11), transparent 25rem), #07060b; }
  .name-grid { display: grid; gap: clamp(2.5rem, 6vw, 6rem); align-items: center; }
  .mini-wordmark { margin: 0; color: #1a1421; font-family: var(--font-display); font-size: clamp(5rem, 17vw, 12rem); font-weight: 650; letter-spacing: -.1em; line-height: .8; text-shadow: 1px 0 var(--ez-pink), -1px 0 var(--ez-cyan); }
  .name-grid > div:last-child > p:last-child { max-width: 45rem; margin: 1.5rem 0 0; }
  .final-cta { border-bottom: 0 !important; text-align: center; }
  .final-cta h2 { max-width: 18ch; margin-inline: auto; }
  .final-cta > :global(div) > p:not(.eyebrow) { max-width: 45rem; margin: 1.5rem auto 0; }
  .final-actions { justify-content: center; }

  @media (min-width: 42rem) {
    .pull-path { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .form-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .lineage-chain, .representation-grid, .status-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .engine-nodes { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  }
  @media (min-width: 64rem) {
    .split-heading { grid-template-columns: minmax(0, 1fr) minmax(24rem, .7fr); align-items: end; }
    .pull-path { grid-template-columns: repeat(6, minmax(0, 1fr)); }
    .lineage-chain { grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 0; }
    .lineage-chain li + li { border-left: 0; }
    .lineage-chain li + li::before { position: absolute; z-index: 2; top: 50%; left: -.38rem; color: var(--ez-cyan); content: "→"; font-family: var(--font-mono); transform: translateY(-50%); }
    .blind-grid, .modes-grid, .collect-grid, .name-grid { grid-template-columns: minmax(0, 1fr) minmax(24rem, .78fr); }
    .modes-grid { grid-template-columns: minmax(25rem, .8fr) minmax(0, 1fr); }
    .system-map { grid-template-columns: minmax(10rem, .35fr) minmax(10rem, .35fr) minmax(0, 1fr); align-items: stretch; }
    .publisher-node::after, .product-node::after { top: 50%; right: -.85rem; bottom: auto; left: auto; width: .85rem; height: 1px; }
    .engine-nodes { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  }
  @media (max-width: 30rem) {
    .primary-action, .secondary-action { width: 100%; }
    .recipe > div { width: 100%; }
  }
  @media (prefers-reduced-motion: reduce) {
    .form-image img { transition: none; }
  }
</style>
