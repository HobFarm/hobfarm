export const academyFeatures = {
  oneTimeCheckoutVisible: false,
  askAcademyEnabled: false,
  askAcademyReason:
    "Ask the Academy remains disabled until at least two paid courses are complete and real unresolved questions show that retrieval would help.",
  sprintApplicationsOpen: false,
} as const;
