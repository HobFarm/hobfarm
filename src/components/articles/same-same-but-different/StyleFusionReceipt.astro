<aside class="stylefusion-receipt" aria-labelledby="stylefusion-receipt-title">
  <p class="stylefusion-receipt__eyebrow">Private working receipt · August 6, 2026</p>
  <h3 id="stylefusion-receipt-title">The package existed before the picture</h3>
  <div class="stylefusion-receipt__route" aria-label="StyleFusion package sequence">
    <span>3 role-scoped references</span>
    <span aria-hidden="true">→</span>
    <span>VisualPromptDocument</span>
    <span aria-hidden="true">→</span>
    <span>provenance and revisions</span>
    <span aria-hidden="true">→</span>
    <span>provider-ready payload</span>
  </div>
  <p>
    The supplied StyleFusion export records subject, style and composition sources, schema and pipeline versions,
    revision metadata and a prepared-payload hash. It records zero generation runs. That makes it evidence for the
    source-package claim, not evidence about any finished image.
  </p>
</aside>

<style>
  .stylefusion-receipt {
    position: relative;
    left: 50%;
    width: min(48rem, 90vw);
    margin: 2.25rem 0;
    padding: 1rem;
    transform: translateX(-50%);
    border: 1px solid color-mix(in srgb, var(--color-accent-400, #9f6cff) 55%, transparent);
    border-left-width: 0.3rem;
    border-radius: 0.35rem;
    background: color-mix(in srgb, var(--color-base-900, #0e0b16) 88%, transparent);
  }

  .stylefusion-receipt__eyebrow,
  .stylefusion-receipt h3,
  .stylefusion-receipt p {
    margin: 0 !important;
  }

  .stylefusion-receipt__eyebrow {
    color: var(--color-accent-300, #be9aff);
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  .stylefusion-receipt h3 {
    margin-top: 0.3rem !important;
    color: var(--color-base-100, #ececec);
    font-size: 1.1rem !important;
  }

  .stylefusion-receipt__route {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem;
    margin: 0.9rem 0;
    color: var(--color-base-300, #c7c7c7);
    font-family: var(--font-mono);
    font-size: 0.68rem;
  }

  .stylefusion-receipt__route span:nth-child(odd) {
    padding: 0.35rem 0.45rem;
    border: 1px solid var(--color-base-700, #393443);
    border-radius: 0.2rem;
  }

  .stylefusion-receipt p:last-child {
    color: var(--color-base-300, #c7c7c7);
    font-size: 0.82rem;
    line-height: 1.6;
  }
</style>
