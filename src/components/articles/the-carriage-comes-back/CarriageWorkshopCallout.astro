<aside class="carriage-workshop article-wide not-prose" aria-labelledby="carriage-workshop-title">
  <p class="carriage-workshop__kicker">Continue in the Workshop</p>
  <h2 id="carriage-workshop-title">Future Carriage: A Concept Campaign from Historical Carriage Design</h2>
  <p>
    The complete project record keeps the source studies, model rules, approved image packet,
    presenter direction, and campaign method together.
  </p>
  <a href="/workshop/future-carriage/">Open the Future Carriage Workshop record <span aria-hidden="true">&rarr;</span></a>
</aside>

<style>
  .carriage-workshop {
    box-sizing: border-box;
    margin-block: 3rem;
    padding: clamp(1.1rem, 3vw, 1.6rem);
    border: 1px solid rgb(138 189 169 / 0.45);
    border-left: 0.35rem solid #c7a25e;
    border-radius: 0.55rem;
    background:
      radial-gradient(circle at 88% 12%, rgb(199 162 94 / 0.14), transparent 18rem),
      #0a100e;
  }

  .carriage-workshop__kicker {
    margin: 0;
    color: #8abda9;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.11em;
    text-transform: uppercase;
  }

  .carriage-workshop h2 {
    max-width: 36rem;
    margin: 0.7rem 0 0;
    color: #f0eadc;
    font-family: var(--font-display);
    font-size: clamp(1.55rem, 4vw, 2.5rem);
    font-weight: 400;
    line-height: 1.08;
  }

  .carriage-workshop > p:not(.carriage-workshop__kicker) {
    max-width: 38rem;
    margin: 0.9rem 0 0;
    color: var(--color-base-300, #c7c7c7);
    line-height: 1.65;
  }

  .carriage-workshop a {
    display: inline-flex;
    gap: 0.45rem;
    align-items: center;
    margin-top: 1.2rem;
    color: #e1bd75;
    font-family: var(--font-mono);
    font-size: 0.76rem;
    text-underline-offset: 0.22em;
  }
</style>
