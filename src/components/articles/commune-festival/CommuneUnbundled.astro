<figure class="commune-unbundled article-wide not-prose" aria-labelledby="commune-unbundled-title">
  <header>
    <p>Social architecture</p>
    <h3 id="commune-unbundled-title">The commune unbundled</h3>
    <div>The original experiment tried to make one institution carry nearly every part of life.</div>
  </header>

  <div class="total-package">
    <span>One total system</span>
    <ul>
      <li>Property</li>
      <li>Housing</li>
      <li>Labor</li>
      <li>Meals</li>
      <li>Money</li>
      <li>Childcare</li>
      <li>Social life</li>
      <li>Ideology</li>
    </ul>
  </div>

  <div class="connector" aria-hidden="true"><span></span></div>

  <div class="descendants" aria-label="Later institutions carrying separate parts of communal life">
    <span>Food co-ops</span>
    <span>Intentional communities</span>
    <span>Cohousing</span>
    <span>Community gardens</span>
    <span>Retreats</span>
    <span>Maker and art spaces</span>
    <span>Earthship neighborhoods</span>
    <span>Temporary festival cities</span>
  </div>

  <figcaption>
    The desire for collective life survived by moving into narrower institutions with clearer boundaries.
  </figcaption>
</figure>

<style>
  .commune-unbundled {
    box-sizing: border-box;
    margin-block: 3rem !important;
    padding: clamp(1rem, 3vw, 1.6rem);
    overflow: hidden;
    border: 1px solid rgb(150 213 193 / 0.28);
    border-radius: 0.65rem;
    background:
      radial-gradient(circle at 50% -15%, rgb(112 187 163 / 0.2), transparent 45%),
      #080a09;
  }

  header {
    max-width: 48rem;
    margin-inline: auto;
    text-align: center;
  }

  header p,
  header h3,
  header div {
    margin: 0 !important;
  }

  header p {
    color: #96d5c1;
    font: 700 0.66rem/1.2 var(--font-mono, monospace);
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  header h3 {
    margin-top: 0.35rem !important;
    color: #f3eee4;
    font-size: clamp(1.45rem, 3vw, 2.2rem);
  }

  header div {
    margin-top: 0.55rem !important;
    color: #aaa69d;
    font-size: 0.82rem;
    line-height: 1.55;
  }

  .total-package {
    width: min(33rem, 100%);
    margin: 1.35rem auto 0;
    padding: 1rem;
    border: 1px solid rgb(240 201 107 / 0.45);
    border-radius: 0.5rem;
    background: rgb(35 30 18 / 0.82);
    box-shadow: 0 0 0.9rem rgb(240 201 107 / 0.08);
  }

  .total-package > span {
    display: block;
    color: #f0c96b;
    font: 700 0.7rem/1.3 var(--font-mono, monospace);
    letter-spacing: 0.09em;
    text-align: center;
    text-transform: uppercase;
  }

  .total-package ul {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.4rem;
    margin: 0.75rem 0 0;
    padding: 0;
    list-style: none;
  }

  .total-package li {
    padding: 0.3rem 0.5rem;
    border: 1px solid rgb(240 201 107 / 0.2);
    border-radius: 999px;
    color: #d8d1c2;
    font-size: 0.72rem;
  }

  .connector {
    position: relative;
    width: 1px;
    height: 2.2rem;
    margin: 0 auto;
    background: rgb(150 213 193 / 0.6);
  }

  .connector::after {
    position: absolute;
    right: -0.28rem;
    bottom: 0;
    width: 0.55rem;
    height: 0.55rem;
    border-right: 1px solid #96d5c1;
    border-bottom: 1px solid #96d5c1;
    content: "";
    transform: rotate(45deg);
  }

  .descendants {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.65rem;
  }

  .descendants span {
    display: grid;
    min-height: 4.4rem;
    place-items: center;
    padding: 0.65rem;
    border: 1px solid rgb(150 213 193 / 0.23);
    border-radius: 0.45rem;
    background: rgb(10 20 17 / 0.84);
    color: #d4d1c9;
    font-size: 0.76rem;
    line-height: 1.4;
    text-align: center;
  }

  figcaption {
    max-width: 45rem;
    margin: 1rem auto 0;
    color: #99968e;
    font-size: 0.75rem;
    line-height: 1.55;
    text-align: center;
  }

  @media (max-width: 50rem) {
    .descendants {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 28rem) {
    .descendants {
      grid-template-columns: 1fr;
    }

    .descendants span {
      min-height: 0;
    }
  }
</style>
