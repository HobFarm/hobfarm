<figure class="sharknado-poster article-wide not-prose">
  <div class="sharknado-poster__layout">
    <img
      src="https://cdn.hob.farm/articles/you-should-write-about-sharks/sharknado-poster-critical-reference.webp"
      alt="The Sharknado poster shows airborne sharks twisting through a tornado above exploding Los Angeles landmarks beneath the large red title Sharknado."
      width="228"
      height="320"
      loading="lazy"
      decoding="async"
      data-lightbox="https://cdn.hob.farm/articles/you-should-write-about-sharks/sharknado-poster-critical-reference.webp"
      data-lightbox-caption="The title, tornado, sharks, and disaster imagery make the complete sales proposition visible before the film begins."
    />
    <figcaption>
      <span class="sharknado-poster__label">The pitch is already finished</span>
      <p>The title names the entire mechanism. The image supplies sharks, tornado, destruction, and scale. The poster does not ask for biological plausibility. It asks whether the combination can be understood before somebody scrolls past it.</p>
      <a href="https://www.theasylum.cc/index.php/titles/detail?id=416" rel="noreferrer">Promotional poster: The Asylum / Syfy →</a>
    </figcaption>
  </div>
</figure>

<style>
  .sharknado-poster {
    margin-block: 3rem;
  }

  .sharknado-poster__layout {
    display: grid;
    grid-template-columns: minmax(180px, 228px) minmax(0, 1fr);
    gap: clamp(1.25rem, 4vw, 3rem);
    align-items: center;
    padding: clamp(1rem, 3vw, 2rem);
    border: 1px solid rgb(238 107 70 / 0.48);
    border-radius: 0.65rem;
    background: linear-gradient(130deg, #08171b, #1f1110);
    box-shadow: 0.65rem 0.75rem 0 rgb(2 9 11 / 0.72);
  }

  .sharknado-poster img {
    display: block;
    width: 100%;
    height: auto;
    border: 1px solid rgb(245 239 227 / 0.34);
    box-shadow: 0.45rem 0.55rem 0 rgb(0 0 0 / 0.48);
  }

  .sharknado-poster figcaption {
    margin: 0;
  }

  .sharknado-poster__label {
    color: #ee6b46;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .sharknado-poster p {
    margin: 0.75rem 0 1rem;
    color: #d4dcdd;
    font-size: 1rem;
    line-height: 1.7;
  }

  .sharknado-poster a {
    color: #8dc7d2;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    text-transform: uppercase;
  }

  @media (max-width: 620px) {
    .sharknado-poster__layout {
      grid-template-columns: 1fr;
    }

    .sharknado-poster img {
      width: min(100%, 228px);
      margin-inline: auto;
    }
  }
</style>
