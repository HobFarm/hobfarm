<figure class="article-wide image-first not-prose">
  <p class="eyebrow">The image arrives first</p>
  <div class="main-path" aria-label="Dalí's described image-making sequence">
    <span>sleep onset</span><i aria-hidden="true">→</i><span>vivid image</span><i aria-hidden="true">→</i><span>wake and retain</span><i aria-hidden="true">→</i><span>careful rendering</span><i aria-hidden="true">→</i><span>later interpretation</span>
  </div>
  <div class="comparison">
    <strong>Message-first side path</strong>
    <span>verbal thesis → illustration</span>
  </div>
  <figcaption>“No message” does not mean no meaning. It changes the order in which meaning enters.</figcaption>
</figure>

<style>
  .image-first {
    margin-block: 2.2rem;
    border-radius: 0.7rem;
    background:
      linear-gradient(110deg, rgb(118 37 49 / 0.32), transparent 52%),
      #0b0c11;
    padding: clamp(1rem, 3vw, 1.5rem);
    box-shadow: inset 0 0 0 1px rgb(216 181 109 / 0.28);
  }

  .eyebrow {
    margin: 0 0 1rem;
    color: #d9b56c;
    font: 700 0.68rem/1.2 var(--font-mono);
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .main-path {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.45rem;
  }

  .main-path span {
    border: 1px solid rgb(92 225 230 / 0.34);
    border-radius: 999px;
    background: rgb(7 20 24 / 0.7);
    padding: 0.48rem 0.72rem;
    color: #e9ffff;
    font: 600 0.72rem/1.2 var(--font-mono);
  }

  i {
    color: #d9b56c;
    font-style: normal;
  }

  .comparison {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 0.8rem;
    margin-top: 1rem;
    border-left: 2px solid rgb(255 255 255 / 0.18);
    padding-left: 0.75rem;
    color: #8f8f94;
    font-size: 0.76rem;
  }

  .comparison strong {
    color: #b8b3ae;
  }

  figcaption {
    margin-top: 1rem;
  }
</style>
