# Doll Family article production record

This directory records the article assets, rights decisions, source audit, upload verification, and browser QA for:

`/articles/they-had-names-doll-family/`

The article is scheduled for **July 26, 2026 at 11:40 p.m. America/Los_Angeles** (`2026-07-27T06:40:00Z`). A one-time GitHub Actions workflow runs five minutes later, changes the article from `scheduled` to `published`, removes itself, and pushes the publication commit to `main` so Cloudflare Pages rebuilds the site.

The exact public appearance time can trail the scheduled minute by GitHub Actions queue time and the Cloudflare Pages build duration.
