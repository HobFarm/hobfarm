name: Publish The Feed Is the Problem

on:
  schedule:
    - cron: "20 23 22 8 *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: publish-the-feed-is-the-problem
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Publish when the scheduled time has arrived
        id: article
        run: node scripts/publish-scheduled-the-feed-is-the-problem.mjs

      - name: Remove the one-time schedule and trigger the production build
        if: steps.article.outputs.published == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/publish-the-feed-is-the-problem.yml
          git rm scripts/publish-scheduled-the-feed-is-the-problem.mjs
          git add src/content/articles/the-feed-is-the-problem.mdx
          git commit -m "Publish The Feed Is the Problem"
          git push origin main
