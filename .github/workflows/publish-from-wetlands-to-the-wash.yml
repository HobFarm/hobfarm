name: Publish From Wetlands to the Wash

on:
  schedule:
    - cron: "20 23 15 8 *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: publish-from-wetlands-to-the-wash
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Publish when the scheduled time has arrived
        id: article
        run: node scripts/publish-scheduled-from-wetlands-to-the-wash.mjs

      - name: Remove the one-time schedule and trigger the production build
        if: steps.article.outputs.published == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/publish-from-wetlands-to-the-wash.yml
          git add src/content/articles/from-wetlands-to-the-wash.mdx
          git commit -m "Publish From Wetlands to the Wash"
          git push origin main
